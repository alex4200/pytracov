from setuptools import setup, find_packages

setup(
    name='pytracov',
    author="Alexander Dietz",
    description="The python-pytest-travis-coverage example repository!",
    long_description="An example repository showing how to setup your code to have travis running as well as the coverage tool, and how to invoke the badges.",
    packages=find_packages()
)